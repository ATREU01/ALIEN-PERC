{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ui && npm install && npm run build",
    "watchPatterns": ["ui/**"]
  },
  "deploy": {
    "startCommand": "cd ui && npm run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
